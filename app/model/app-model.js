// ResortMate = new Mongo.Collection("resort-mate");

// if (Meteor.isClient) {
//   Meteor.subscribe("resort-mate");
// }

// if (Meteor.isServer) {
//   Meteor.publish("resort-mate", function() {
//     return ResortMate.find({});
//   });
// }
